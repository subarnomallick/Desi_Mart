{
  "name": "deshimart",
  "version": "1.0.0",
  "private": true,
  "description": "DeshiMart - Farmers Direct Marketplace & AI Agronomist Platform",
  "type": "module",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend --include=dev",
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "ecommerce",
    "farmers",
    "marketplace",
    "render",
    "react",
    "express",
    "mongodb",
    "razorpay",
    "upi"
  ],
  "author": "Subarno Mallick",
  "license": "ISC"
}
